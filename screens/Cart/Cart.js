import React from 'react';
import {
    View,
    Text,
    Image,
} from  'react-native'

var Cart = (props) => {


    return <View style={{flex: 1, flexDirection: 'row'}}>
        <Image source={{}}/>
    </View>
}

export default Cart