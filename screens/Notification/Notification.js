import React from 'react'
import {
    View
} from 'react-native'
import {UIHeader} from '../../components'

var Notification = (props) => {
    return <View>
        <UIHeader title="Notification"/>
    </View>
}

export default Notification